name: CI — Backend

on:
  push:
    paths:
      - "backend/**"
  pull_request:
    paths:
      - "backend/**"

jobs:
  test:
    name: Test & audit
    uses: ./.github/workflows/_test-java.yaml
    with:
      working-directory: ./backend
